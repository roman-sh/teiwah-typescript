# MediaType

`ptt` is a WhatsApp voice note; `audio` is a generic audio attachment.

## Example Usage

```typescript
import { MediaType } from "teiwah/models";

let value: MediaType = "document";
```

## Values

```typescript
"image" | "ptt" | "audio" | "video" | "document"
```